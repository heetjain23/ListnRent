{
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then export VITE_SITE_MODE=production; elif [ \"$VERCEL_ENV\" = \"preview\" ]; then export VITE_SITE_MODE=preview; else export VITE_SITE_MODE=development; fi; npm run build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
